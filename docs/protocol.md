# ManT JSON Protocol and Schema Reference

The versioned protocol reference is now maintained as the installable [mant-protocol(5) manual](manuals/mant-protocol.md).

This compatibility page is intentionally retained so links to the former reference location continue to resolve.
