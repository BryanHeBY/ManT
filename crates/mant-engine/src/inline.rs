//! Shared operations over source-independent inline IR nodes.

pub(crate) use mant_ir::{
    first_visible_character, has_printable_character, inline_plain_text as plain_text,
    last_visible_character, terms_fit_inline,
};
