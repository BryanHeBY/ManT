diff --git a/read.c b/read.c
--- a/read.c
+++ b/read.c
@@ -23,7 +23,6 @@
  * and steering of the various parsers.
  */
 #include "config.h"
-#include "mant_thread_local.h"
 
 #include <sys/types.h>
 #ifndef MANDOC_MEMORY_ONLY
@@ -75,6 +74,7 @@ struct	mparse {
 	int		  while_count; /* finite .while expansion */
 	int		  while_total; /* aggregate .while replays */
 	int		  while_total_reported;
+	int		  input_depth; /* shared memory/file recursion */
 	int		  line; /* line number in the file */
 };
 
@@ -620,22 +620,20 @@ void
 mparse_readmem(struct mparse *curp, const unsigned char *data, size_t size,
     const char *filename)
 {
-	MANT_THREAD_LOCAL int recursion_depth;
-
 	struct buf	 blk;
 	struct buf	*save_primary;
 	const char	*save_filename, *cp;
 	size_t		 offset;
 	int		 save_filenc, save_lineno;
 
-	if (recursion_depth == 0) {
+	if (curp->input_depth == 0) {
 		curp->while_total = 0;
 		curp->while_total_reported = 0;
 	}
-	if (recursion_depth > 64) {
+	if (curp->input_depth > 64) {
 		mandoc_msg(MANDOCERR_ROFFLOOP, curp->line, 0, NULL);
 		return;
-	} else if (recursion_depth == 0) {
+	} else if (curp->input_depth == 0) {
 		if ((cp = strrchr(filename, '.')) != NULL &&
 		    cp[1] >= '1' && cp[1] <= '9')
 			curp->man->filesec = cp[1];
@@ -662,9 +660,9 @@ mparse_readmem(struct mparse *curp, const unsigned char *data, size_t size,
 	} else
 		offset = 0;
 
-	recursion_depth++;
+	curp->input_depth++;
 	mparse_buf_r(curp, blk, offset, 1);
-	if (--recursion_depth == 0)
+	if (--curp->input_depth == 0)
 		mparse_end(curp);
 
 	curp->primary = save_primary;
@@ -681,8 +679,6 @@ mparse_readmem(struct mparse *curp, const unsigned char *data, size_t size,
 void
 mparse_readfd(struct mparse *curp, int fd, const char *filename)
 {
-	MANT_THREAD_LOCAL int recursion_depth;
-
 	struct buf	 blk;
 	struct buf	*save_primary;
 	const char	*save_filename, *cp;
@@ -690,14 +686,14 @@ mparse_readfd(struct mparse *curp, int fd, const char *filename)
 	int		 save_filenc, save_lineno;
 	int		 with_mmap;
 
-	if (recursion_depth == 0) {
+	if (curp->input_depth == 0) {
 		curp->while_total = 0;
 		curp->while_total_reported = 0;
 	}
-	if (recursion_depth > 64) {
+	if (curp->input_depth > 64) {
 		mandoc_msg(MANDOCERR_ROFFLOOP, curp->line, 0, NULL);
 		return;
-	} else if (recursion_depth == 0 &&
+	} else if (curp->input_depth == 0 &&
 	    (cp = strrchr(filename, '.')) != NULL &&
             cp[1] >= '1' && cp[1] <= '9')
                 curp->man->filesec = cp[1];
@@ -731,9 +727,9 @@ mparse_readfd(struct mparse *curp, int fd, const char *filename)
 	} else
 		offset = 0;
 
-	recursion_depth++;
+	curp->input_depth++;
 	mparse_buf_r(curp, blk, offset, 1);
-	if (--recursion_depth == 0)
+	if (--curp->input_depth == 0)
 		mparse_end(curp);
 
 	/*
@@ -826,6 +822,7 @@ mparse_reset(struct mparse *curp)
 	free_buf_list(curp->secondary);
 	curp->secondary = NULL;
 	curp->gzip = 0;
+	curp->input_depth = 0;
 	tag_alloc();
 }
 
