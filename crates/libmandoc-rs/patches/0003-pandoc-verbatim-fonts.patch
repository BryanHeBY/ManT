diff --git a/mandoc.c b/mandoc.c
--- a/mandoc.c
+++ b/mandoc.c
@@ -48,0 +49,3 @@ mandoc_font(const char *cp, int sz)
+		case 'C':
+		case 'V':
+			return ESCAPE_FONTCR;
@@ -85,0 +89,9 @@ mandoc_font(const char *cp, int sz)
+		case 'V':
+			switch (cp[1]) {
+			case 'B':
+				return ESCAPE_FONTCB;
+			case 'I':
+				return ESCAPE_FONTCI;
+			default:
+				return ESCAPE_ERROR;
+			}
