--- a/preconv.c
+++ b/preconv.c
@@ -64,6 +64,10 @@ preconv_encode(const struct buf *ib, size_t *ii, struct buf *ob, size_t *oi,
 		goto latin;
 	}
 
+	/* Memory-only callers need not provide trailing sentinel bytes. */
+	if ((size_t)nby > ib->sz - *ii)
+		goto latin;
+
 	cu++;
 	switch (nby) {
 	case 3:
