Subject: keep ohash size unsigned
Upstream-Status: Candidate (not submitted)
Replaces: 0025

Match the ohash size field width instead of relying on size_t narrowing.

--- a/compat_ohash.c
+++ b/compat_ohash.c
@@ -73,7 +73,7 @@
 ohash_resize(struct ohash *h)
 {
 	struct _ohash_record *n;
-	size_t ns;
+	unsigned int ns;
 	unsigned int	j;
 	unsigned int	i, incr;
 
