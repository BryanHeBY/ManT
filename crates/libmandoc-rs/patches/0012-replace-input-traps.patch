--- a/roff.c
+++ b/roff.c
@@ -3302,6 +3302,7 @@ roff_it(ROFF_ARGS)
 	 */
 
 	roffit_lines = iv;
+	free(roffit_macro);
 	roffit_macro = mandoc_strdup(iv != 1 ||
 	    strcmp(buf->buf + pos, "an-trap") ?
 	    buf->buf + pos : "br");
