[package]
name = "libmandoc-rs"
version = "0.9.0"
edition.workspace = true
rust-version.workspace = true
license = "Apache-2.0 AND ISC AND BSD-2-Clause AND BSD-3-Clause"
description = "Safe Rust interface to the vendored libmandoc parser and reference renderers"
repository.workspace = true
homepage.workspace = true
readme = "README.md"
keywords = ["man", "mdoc", "roff", "mandoc", "parser"]
categories = ["parser-implementations", "text-processing"]
include = [
  "/Cargo.toml",
  "/README.md",
  "/THIRD_PARTY_NOTICES.md",
  "/LICENSES/**",
  "/build.rs",
  "/benches/**",
  "/config/**",
  "/shim/**",
  "/src/**",
  "/tests/**",
  "/vendor/mandoc-1.14.6/**",
  "!/vendor/mandoc-1.14.6/soelim.c",
  "!/vendor/mandoc-1.14.6/soelim.1",
]
publish.workspace = true
links = "libmandoc_rs"

[package.metadata.docs.rs]
all-features = true

[features]
default = []
render = ["dep:unicode-width"]
serde = ["dep:serde"]

[dependencies]
flate2.workspace = true
serde = { workspace = true, optional = true }
unicode-width = { workspace = true, optional = true }
zstd.workspace = true

[target.'cfg(windows)'.dependencies]
windows-sys.workspace = true

[build-dependencies]
cc = "1"

[dev-dependencies]
serde_json.workspace = true

[lints]
workspace = true

[[bench]]
name = "ast_transfer"
harness = false
