#!/usr/bin/env bash
# Reconstruct the one active vendor tree from its locked source and patch series.
# End-user Cargo builds compile checked-in sources and never run this script.
set -euo pipefail
exec python3 "$(dirname "$0")/sync_vendor.py" "$@"
